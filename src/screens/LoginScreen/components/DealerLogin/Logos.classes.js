import { StyleSheet } from 'aphrodite/no-important';

const classes = StyleSheet.create({
  logos: {
    width: '100%',
    padding: '10px 60px 0',
  },
  logos__logo: {
    maxWidth: '100%',
  },
});

export default classes;
