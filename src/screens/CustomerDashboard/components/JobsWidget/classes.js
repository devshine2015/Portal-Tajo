import { StyleSheet } from 'aphrodite/no-important';

export default StyleSheet.create({
  wrapper: {
    width: '70%',
    transition: 'all .5s',
  },
  wrapper_full: {
    width: '100%',
  },
  inn: {
    display: 'flex',
    flexDirection: 'column',
    minHeight: 300,
  },
});
