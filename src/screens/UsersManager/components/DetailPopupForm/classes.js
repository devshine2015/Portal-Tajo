import { StyleSheet } from 'aphrodite/no-important';

const classes = StyleSheet.create({
  form: {
    position: 'relative',
    padding: 5,
  },

  loaderWrapper: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    backgroundColor: '#fff',
    fontSize: 18,
  },
});

export default classes;
