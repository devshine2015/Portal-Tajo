import { StyleSheet } from 'aphrodite/no-important';

export default StyleSheet.create({
  wrapper: {
    width: '100%',
  },
  inn: {
    display: 'flex',
    flexDirection: 'column',
    minHeight: 300,
  },
});
