import { StyleSheet } from 'aphrodite/no-important';

const classes = StyleSheet.create({
  intervals: {
    display: 'flex',
    alignItems: 'center',
  },

  dilimiter: {
    fontSize: 14,
    color: '#aaa',
    margin: '0 10px',
  },

  picker: {
    width: 150,
    marginBottom: 5,
  },

  filter__wrapper: {
    display: 'flex',
    alignItems: 'center',
  },
});

export default classes;
