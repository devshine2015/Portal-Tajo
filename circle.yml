machine:
  node:
    version: 5.7.0

general:
  branches:
    ignore:
      - dev

deployment:
  production:
    branch: stage
    commands:
      - ./deploy.sh 'drvrappbeta@drvrappbeta.cloudapp.net'
  dev:
    branch: dev-release
    commands:
      - ./deploy.sh 'ddsdev@ddsdev.cloudapp.net'
  ssreportsDev:
    branch: ssreports-dev
    commands:
      - ./deployDemo.sh 'ddsdev@ddsdev.cloudapp.net'
  ssreportsProd:
    branch: ssreports-prod
    commands:
      - ./deployDemo.sh 'drvrappbeta@drvrappbeta.cloudapp.net'
  # demo:
  #   branch: demo
  #   commands:
  #     - ./deployDemo.sh 'drvrappbeta@drvrappbeta.cloudapp.net'