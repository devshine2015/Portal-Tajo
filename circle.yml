machine:
  node:
    version: 5.7.0

general:
  branches:
    ignore:
      - master
      - /sandbox-.*/ # ignore all branches with sandbox prefix; like sandbox-feature1

deployment:
  dev:
    branch: dev-release
    commands:
      - ./ci/deploy-dev.sh 'ddsdev@ddsdev.cloudapp.net' 'tajo' 'portal'
  production:
    branch: production
    commands:
      - ./ci/deploy-prod.sh 'drvrappbeta@drvrappbeta.cloudapp.net' 'tajo' 'portal'
  ssreportsDev:
    branch: ssreports-dev
    commands:
      - ./deployToOld.sh 'ddsdev@ddsdev.cloudapp.net'
  ssreportsProd:
    branch: ssreports-prod
    commands:
      - ./deployToOld.sh 'drvrappbeta@drvrappbeta.cloudapp.net'
