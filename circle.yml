machine:
  node:
    version: 6.2.2

general:
  branches:
    ignore:
      - master
      - verified_candidate

deployment:
  dev:
    branch: dev
    commands:
      - ./ci/deployToDev.sh
    # Maksim's personal branch
    branch: dev@max
    commands:
      - ./ci/deployToDev.sh 'max'
    # Kirill's personal branch
    branch: dev@karl
    commands:
      - ./ci/deployToDev.sh 'karl'

  stage:
    branch: stage
    commands:
      - ./ci/deploy.sh 'tajo' 'portal'
  production:
    branch: production
    commands:
      - ./ci/deploy.sh 'drvrprod@drvrprod2216.cloudapp.net' 'tajo' 'portal' '~/driver-server/static-root/public'
